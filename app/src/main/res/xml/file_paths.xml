<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- 下载的 APK 存放在 cache/download 目录 -->
    <cache-path name="download" path="download/" />
    <!-- 导出的日志文件存放在 cache/logs 目录 -->
    <cache-path name="logs" path="logs/" />
</paths>