# HANDOFF — 读全文再开始干活

生成时间: 2026-08-17T22:07:49+08:00 · Git HEAD: `8feb705`
信任规则: [V] = 交接时已用命令验证；[?] = 仅记忆未复核，当线索对待；[X] = 已证伪，别用。

## 0. 复核（下一会话先做）
- 锚点: `main` @ `8feb705` (2026-08-17)
- 漂移检查: `git rev-parse HEAD` 是否仍 = `8feb705`；变了说明快照可能过期
- 先读: `HANDOFF.md` + `CLAUDE.md`

## 1. 当前目标

**修复 LSPosed 模块仓库显示 1970 年 + 无法置顶 + README 不跟着更新。** 已完成：README 全面同步 Beta 4（源仓库 + 模块仓库）、sync workflow 加 README 同步步骤、修复 `gh release edit --name` bug。遗留：1970/置顶是 Beta 阶段固有现象（`latestReleaseTime` 只认 stable release），等正式版解决；position 合并修复待用户真机确认；janky 根因待排查。

## 2. 已验证状态 — 工作实际停在哪

- [V] **README 全面同步 Beta 4** — `82ee120` 更新源仓库 README 8 处：badge、响应曲线（线性/自定义）、版本门控（8.0.4/200146）、已测试版本表（删 8.0.0）、配置页表格、技术栈（Kotlin 2.4.10/AGP 9.3.1/Gradle 9.6.1）、设置页表格、版本历史顶部。
- [V] **sync workflow 加 README 同步** — `38f128a` 在 `sync-lsposed-mirror.yml` 加 `Sync README to mirror` 步骤：读源仓库 README → 追加 `**Source URL:**` 结尾（幂等）→ PUT 到模块仓库。
- [V] **修复 `gh release edit --name` bug** — `8feb705` 改为 `--title`。全量同步时 edit 分支暴露（tag 触发只走 create 分支，之前没暴露）。
- [V] **模块仓库 README 已同步** — 手动触发 sync workflow（run `32037614134`）全绿，模块仓库 README 更新到 Beta 4（commit `4afaab70`），Source URL 结尾保留。
- [V] **1970/置顶根因确认** — LSPosed `gatsby-node.js` 的 `latestReleaseTime` 只认 stable release（`find(!isPrerelease)`），找不到就保持默认 `1970-01-01T00:00:00Z`。我们 6 个 release 全是 prerelease，所以显示 1970、列表排最后（排序第一键 `latestReleaseTime DESC`）。**无法通过 workflow 修复，等正式版。**
- [V] **工作区干净** — `git status` 无未提交改动，`main` 与 `origin/main` 同步。
- [?] **position 合并修复** — 上一会话 `resolveLatestSettings()` 从本地 externalFilesDir 合并 position，`mergePositionFromLocalPublic()` 公开化。用户未确认是否生效。
- [?] **切换模式后位置丢失** — 用户反馈"启动时调过位置大小的单踏板，切双踏板再切回单踏板，位置/大小变成默认状态"。上述 position 合并修复已尝试解决，用户未确认。

### 测试/build 输出（本次交接 run 的真实输出）
```
gh run watch 32037614134 → ✓ sync workflow 全绿（6 个 release 同步 + README 同步）
gh run view 32037614134 --log → "Synced README -> Xposed-Modules-Repo/tools.alamobile.mod"
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/sync-lsposed-mirror.yml'))" → YAML OK
gh api repos/Xposed-Modules-Repo/tools.alamobile.mod/contents/README.md → Beta 4 状态 + Source URL 结尾
```

## 3. 决策与理由

- **README 同步保留 Source URL 结尾** [V]——模块仓库 README 结尾的 `**Source URL:**` 是 LSPosed 规范，源仓库没有。同步时幂等追加（`grep -q` 判断已存在则不重复）。
- **`gh release edit` 用 `--title` 而非 `--name`** [V]——`gh release edit` 的 flag 是 `--title`，`--name` 不存在。全量同步时 edit 分支暴露。
- **1970/置顶接受现状** [V]——`latestReleaseTime` 只认 stable release，Beta 阶段无法置顶。用户选择等正式版，不强行发 stable。
- **`/release` skill 步骤 5C 更新** [V]——发版时同步 README 到 Beta 4 状态（对照代码逐项核对），commit 时带上 README.md。

## 4. 失败的尝试 — 不要再试

- [X] **`useLegacyPackaging = true`** — M40 照搬 KernelSU 加的，导致 `libshadowhook.so not found` 的 native 加载失败。不要再加。
- [X] **`readFromTargetProcess` 作为 rebuild 唯一配置来源** — daemon 写入滞后于广播，rebuild 读到旧 pedalMode/curve。`rebuildFromConfigChange` 和 `toggleOverlays` 必须优先用广播 JSON。不要再单用 `readFromTargetProcess`。
- [X] **手写 SwitchRow/SliderRow → miuix preference 组件** — M38 M39 已验证，换了仍 22-38% janky。
- [X] **关 blur / 移除 rememberContentReady / ModConfig.read 异步 / 各种 janky 优化** — 所有 M38-M40 的 janky 修复尝试均无效，详见旧 HANDOFF.md。
- [X] **手工 `gh release edit` 事后覆盖镜像 body** — 永远晚于 CI 内 sync（`sync-lsposed needs: build`），镜像拿到默认安装说明。不要再依赖事后覆盖，必须 Notes 先于 CI 存在。
- [X] **`gh release edit --name`** — flag 不存在，全量同步时 edit 分支报 `unknown flag: --name`。用 `--title`。

## 5. 已知坑

- ⚠️ **daemon 配置写入滞后于广播** [V]——`ModConfig.write` 先写 remote preferences（daemon），再发广播。但 daemon 异步绑定可能延迟，广播比 remote 先到。`readFromTargetProcess` 读 remote（daemon 旧值）≠ 刚写入的配置。解决方案：`rebuildFromConfigChange` 和 `toggleOverlays` 优先用广播 JSON。
- ⚠️ **广播 JSON 不含 position 字段** [V]——ConfigActivity 不管 position（游戏进程拖拽时 `saveOverlayPosition` 写本地）。用广播 JSON 解析 `Settings` 后必须从本地 externalFilesDir 合并 position，否则重建后位置/大小丢回默认。`resolveLatestSettings()` 已处理此逻辑。
- ⚠️ **AGP 9 不需要 kotlin-android 插件** [V]——AGP 9 内置 Kotlin 支持，`org.jetbrains.kotlin.android` 插件会报错。
- ⚠️ **NDK 29 下载失败** [V]——Clash TUN TLS 干扰，用本地 NDK 26 替代。
- ⚠️ **miuix SwitchPreference 在我们的 app 中 22% janky，KSU 同版本 0.10%** [V]——仍待排查，R8 优化差异可能。
- ⚠️ **lint 的 NewApi 检查会拦 minSdk 26 下的高版本 API** [V]——照搬 KernelSU 代码时注意 KernelSU minSdk 更高，其 API 调用可能超出我们的 minSdk。新增高版本 API 调用时用 `values-vNN` 拆分或 `SDK_INT` 守卫。
- ⚠️ **镜像仓库 Release Notes 时序** [V]——已修复（Notes 先于 CI 存在），但发版必须走 `/release` skill 步骤 5C：写 `RELEASE_NOTES.md` → 同步版本号 → 同步 README → commit + tag + push。手动打 tag 没写 RELEASE_NOTES.md 时 CI 会 fallback 到默认安装说明。
- ⚠️ **LSPosed `latestReleaseTime` 只认 stable** [V]——Beta/Alpha 阶段 `latestReleaseTime` 永远是 `1970-01-01T00:00:00Z`，列表排最后。只有发 stable release 才能置顶。这是 LSPosed 生态规则，不是我们的 bug。

## 6. 下一步（有序）

1. **验证 position 合并修复** — 用户确认"切双踏板再切回单踏板，位置/大小是否保持"。如果仍丢回默认，需排查 `resolveLatestSettings()` 的 `mergePositionFromLocalPublic` 是否正确合并了 position。
2. **继续排查 janky 根因** — R8 映射文件对比（KSU dex=5.2MB vs 我们 2MB），见 M40 HANDOFF。

## 7. 留给用户的开放问题

- 切换模式后单踏板位置丢失问题是否已修复？需要用户真机验证。
- 自定义曲线图表布局（分隔线/卡片结构/轴标签位置）是否满意？之前用户说"不改了"，但交互细节可继续调整。
- 1970/置顶问题等正式版解决——是否计划近期发 stable release？
